# .1024 Configuration file for project run commands, compilation and debug settings (optional);
# Any changes made will be auto-saved and take effect immediately.
# For more information, please refer to the documentation: https://docs.clacky.ai/clacky-workspace/configure

# Run project in development mode - launches browser-sync server for live reloading
run_command: browser-sync start --server --no-notify --no-open --files '**/*.css, **/*.html, **/*.js'

# Note: No compile_command needed as this is a pure HTML/CSS/JS project that runs directly in browser
# Note: No dependency_command needed as all required tools are pre-installed in Clacky environment